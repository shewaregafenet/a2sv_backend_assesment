const { DataTypes} = require("sequelize")

const ingridient = require("../database/sequelize",
      {  name: {
            type: DataTypes.STRING,
        },
    }

    
)

module.exports = ingridient
